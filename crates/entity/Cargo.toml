[package]
name = "entity"
edition.workspace = true
authors.workspace = true
version.workspace = true

[lib]
name = "entity"
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
sea-orm = { version = "0.12.3"}
